<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
  </ItemGroup>
  <!-- Use the runtime-provided System.Text.Json from the SDK instead of adding an explicit package here.
       This avoids pulling an advisory version into the tools folder. The tool targets net8.0 so the
       System.Text.Json APIs are available via the framework. -->

</Project>
