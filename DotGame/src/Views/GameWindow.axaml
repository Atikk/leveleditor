<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="DotGameAvalonia.Views.GameWindow"
        Title="DotGame – Play Test"
        Width="850" Height="650"
        WindowStartupLocation="CenterScreen"
        CanResize="True">
    
    <Grid Background="White">
        <Canvas Name="GameCanvas" Background="White" />
        
        <Border Name="CombatUI" IsVisible="False" Background="#F0F0F0" Padding="20"
                HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,50">
            <StackPanel Spacing="10" MinWidth="400">
                <TextBlock Name="TxtCombatMessage" Text="Combat!" Foreground="Black" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" />
                
                <Grid ColumnDefinitions="*,*">
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Name="TxtPlayerInfo" Foreground="Black" FontSize="14" />
                        <ProgressBar Name="BarPlayerHP" Height="20" Minimum="0" Maximum="100" Value="100" Foreground="Green" />
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Spacing="5">
                        <TextBlock Name="TxtEnemyInfo" Foreground="Black" FontSize="14" HorizontalAlignment="Right" />
                        <ProgressBar Name="BarEnemyHP" Height="20" Minimum="0" Maximum="100" Value="100" Foreground="Red" />
                    </StackPanel>
                </Grid>
                
                <StackPanel Name="PanelCombatActions" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10">
                    <Button Name="BtnAttack" Content="⚔️ Attack" Width="120" Height="40" Background="#E0E0E0" Foreground="Black" />
                    <Button Name="BtnDefend" Content="🛡️ Defend" Width="120" Height="40" Background="#E0E0E0" Foreground="Black" />
                </StackPanel>
            </StackPanel>
        </Border>
        
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Spacing="5">
            <TextBlock Name="TxtPlayerStats" Foreground="Black" FontSize="14" />
        </StackPanel>
    </Grid>
    
</Window>
